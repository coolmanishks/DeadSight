<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.subham.deadsight">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".admin_sign_in" />
        <activity android:name=".admin_project_list" />
        <activity android:name=".contracter_sign_in" />
        <activity android:name=".supervisor_sign_in" />
        <activity android:name=".admin_create_account" />
        <activity android:name=".forgot_password" />
    </application>

</manifest>